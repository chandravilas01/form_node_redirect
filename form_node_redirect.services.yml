services:
  form_node_redirect.form_node_redirect_services:
    class: Drupal\form_node_redirect\FormNodeRedirectServices
    arguments: ['@config.factory', '@path.validator', '@entity_type.bundle.info']
